<mvc:View controllerName="ehsm.controller.IncidentManagement"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page title="Incident Management" showNavButton="true" navButtonPress="onNavBack">
        <content>
            <Table id="incidentTable" items="{/Z898_INCIDENT}" growing="true" growingThreshold="20" width="auto" class="sapUiResponsiveMargin">
                <headerToolbar>
                    <Toolbar>
                        <Title text="Incidents Overview" level="H2"/>
                        <ToolbarSpacer/>
                        <SearchField placeholder="Search Incidents" width="30%"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column><Text text="Incident ID"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Plant"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Description"/></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Category"/></Column>
                    <Column><Text text="Priority"/></Column>
                    <Column><Text text="Status"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Date"/></Column>
                    <Column minScreenWidth="Tablet" demandPopin="true"><Text text="Time"/></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Completion Date"/></Column>
                    <Column minScreenWidth="Desktop" demandPopin="true"><Text text="Created By"/></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{IncidentId}"/>
                            <Text text="{Plant}"/>
                            <Text text="{IncidentDescription}"/>
                            <Text text="{IncidentCategory}"/>
                            <ObjectStatus text="{IncidentPriority}" state="{path: 'IncidentPriority', formatter: '.formatter.priorityState'}"/>
                            <ObjectStatus text="{IncidentStatus}" state="{path: 'IncidentStatus', formatter: '.formatter.statusState'}"/>
                            <Text text="{path: 'IncidentDate', type: 'sap.ui.model.type.Date', formatOptions: { style: 'medium' }}"/>
                            <Text text="{path: 'IncidentTime', type: 'sap.ui.model.type.Time', formatOptions: { style: 'short' }}"/>
                            <Text text="{path: 'CompletionDate', type: 'sap.ui.model.type.Date', formatOptions: { style: 'medium' }}"/>
                            <Text text="{CreatedBy}"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
